"use client";

import React from "react";
import AppSidebar, { AppSidebarProps } from "./AppSidebar";

export default function ProctorSidebar(props: AppSidebarProps) {
  return <AppSidebar {...props} />;
}

export { AppSidebar };
